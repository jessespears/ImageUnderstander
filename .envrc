# Automatically set AWS profile when entering this directory
# Requires direnv: https://direnv.net/
# Install: brew install direnv
# Setup: Add to ~/.zshrc: eval "$(direnv hook zsh)"
# Usage: Run 'direnv allow' in this directory

export AWS_PROFILE=AdministratorAccess-695875594847

echo "✓ AWS_PROFILE set to: $AWS_PROFILE"
echo "✓ Account: 695875594847"
echo "✓ Ready to deploy ImageUnderstander infrastructure"
